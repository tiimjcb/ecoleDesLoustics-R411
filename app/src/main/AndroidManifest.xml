<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoleDesLoustics"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateUserActivity"/>
        <activity android:name=".SelectExerciseActivity"/>
        <activity android:name=".additions.AdditionActivity"/>
        <activity android:name=".additions.AdditionCorrectionActivity" />
        <activity android:name=".multiplications.MultiplicationActivity"/>
        <activity android:name=".multiplications.MultiplicationCorrectionActivity" />
        <activity android:name=".quizzFrancais.FrancaisSplashScreenActivity" />
        <activity android:name=".quizzFrancais.FrancaisQuizActivity" />
        <activity android:name=".quizzFrancais.FrancaisQuizResultatActivity" />
        <activity android:name=".calcul.CorrectionCalculActivity" />
        <activity android:name=".calcul.CalculActivity" />
    </application>

</manifest>